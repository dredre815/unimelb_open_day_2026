# This static GitHub Pages build does not read an API key at build time.
# The event operator enters a restricted, short-lived project key in the in-app setup screen.
NEXT_PUBLIC_DEMO_BUILD_LABEL=Open Day 2026
